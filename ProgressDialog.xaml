<Window x:Class="ClearGlass.ProgressDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Clear Glass Progress" Height="200" Width="500"
        WindowStartupLocation="CenterScreen"
        Background="Transparent"
        AllowsTransparency="True"
        WindowStyle="None"
        BorderThickness="1"
        BorderBrush="{StaticResource GlassBorder}"
        ResizeMode="NoResize">
    
    <Border Background="{StaticResource GlassBackground}"
            CornerRadius="10"
            BorderThickness="1"
            BorderBrush="{StaticResource GlassBorder}">
        <Border.Effect>
            <DropShadowEffect ShadowDepth="0" BlurRadius="20" Color="#40000000"/>
        </Border.Effect>
        
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <!-- Title -->
            <TextBlock Grid.Row="0" 
                       Text="Clear Glass Experience"
                       FontSize="18" 
                       FontWeight="Bold"
                       Foreground="{StaticResource TextPrimary}"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,15"/>
            
            <!-- Status Text -->
            <TextBlock Grid.Row="1" 
                       x:Name="StatusText"
                       Text="Initializing..."
                       FontSize="14"
                       Foreground="{StaticResource TextSecondary}"
                       HorizontalAlignment="Center"
                       TextWrapping="Wrap"
                       Margin="0,0,0,15"/>
            
            <!-- Progress Bar -->
            <Border Grid.Row="2" 
                    Height="8"
                    Background="#20FFFFFF"
                    BorderThickness="0"
                    CornerRadius="4"
                    Margin="0,0,0,15">
                <ProgressBar x:Name="ProgressBar"
                           Background="Transparent"
                           Foreground="{StaticResource PrimaryBlue}"
                           BorderThickness="0"/>
            </Border>
            
            <!-- Progress Percentage -->
            <TextBlock Grid.Row="3" 
                       x:Name="ProgressText"
                       Text="0%"
                       FontSize="12"
                       Foreground="{StaticResource TextSecondary}"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"/>
        </Grid>
    </Border>
</Window> 